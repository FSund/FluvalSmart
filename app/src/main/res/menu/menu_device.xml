<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_device_find"
        android:icon="@mipmap/ic_find"
        android:title=""
        app:showAsAction="always"/>
    <item
        android:id="@+id/menu_device_edit"
        android:icon="@mipmap/ic_edit"
        android:title="@string/rename"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/menu_device_modify_psw"
          android:icon="@drawable/ic_modify_password"
          android:title="@string/modify_password"
          android:visible="false"
          app:showAsAction="ifRoom"/>

</menu>